package fs_project.model.Attributes;


/**
 * The enum User role. Used for access validation
 */
public enum UserRole {
    /**
     * Admin user role.
     */
    ADMIN,
    /**
     * User user role.
     */
    USER
}
